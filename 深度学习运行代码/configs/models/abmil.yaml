_base_: ../default.yaml

experiment_name: abmil_resnet50

# Attention-based MIL (Ilse et al., 2018) without the sigmoid gate.
model:
  name: abmil
  in_dim: 2048
  attention_hidden: 256
  attention_bias: false
  mlp_hidden: 256
  dropout: 0.5
  n_outputs: 1
